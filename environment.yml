name: xcube-stac
channels:
  - conda-forge
dependencies:
  # Required
  - python>=3.10
  - boto3
  - dask
  - numpy
  - rasterio
  - rioxarray
  - requests
  - pandas
  - pystac
  - pyproj
  - pystac-client
  - shapely
  - xarray
  - xcube >= 1.7.0
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
