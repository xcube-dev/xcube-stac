name: xcube-stac
channels:
  - conda-forge
  - defaults
dependencies:
  # Required
  - python>=3.11
  - pystac
  - pystac-client
  - xarray
  - xcube
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
