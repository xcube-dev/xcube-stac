name: xcube-stac
channels:
  - conda-forge
  - defaults
dependencies:
  # Required
  - pystac
  - pystac-client
  - xarray
  - xcube
  # for testing
  - black
  - flake8
  - pytest
  - pytest-recording
