name: xcube-stac
channels:
  - conda-forge
dependencies:
  # Required
  - python>=3.10
  - odc-geo
  - odc-stac
  - pandas
  - pystac
  - pystac-client
  - xarray
  - xcube >= 1.7.0
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
