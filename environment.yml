name: xcube-stac
channels:
  - conda-forge
dependencies:
  # Required
  - python>=3.10
  - boto3
  - dask
  - numpy
  - libgdal-jp2openjpeg
  - rasterio
  - rioxarray
  - requests
  - pandas
  - pystac
  - pyproj
  - pystac-client
  - shapely
  - xarray
  - xmltodict
  - xcube >= 1.8.1
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
