name: xcube-stac
channels:
  - conda-forge
dependencies:
  # Required
  - python>=3.10
  - boto3
  - dask
  - s3fs
  - numpy
  - odc-stac
  - rasterio
  - rioxarray
  - requests
  - pandas
  - pystac
  - pyproj
  - pystac-client
  - shapely
  - tqdm
  - xarray
  - xcube >= 1.7.0
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
